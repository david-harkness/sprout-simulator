source "https://rubygems.org"

ruby "2.6.3"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'parallel'
group :development do
  gem 'guard'
  gem 'pry'
  gem 'curses'
end


# Specify your gem's dependencies in sprout_simulator.gemspec
gemspec
